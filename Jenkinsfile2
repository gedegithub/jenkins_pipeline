pipeline {
    agent any

    stages {
        stage('List Files') {
            steps {
                sh '''
                cd /
                ls
                '''
            }
        }
    }
}
